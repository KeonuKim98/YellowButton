import { Stack } from '@mui/material';

export const Home = () => {
  return (
    <Stack
      sx={{
        height: '100%',
        width: '100%',
        backgroundColor: '#900020',
      }}
    ></Stack>
  );
};
